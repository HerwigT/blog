---
import {Icon} from 'astro-icon/components'
const { platform, username, icon } = Astro.props;
---
<a class="hover:scale-110 focus:scale-110 py-2 text-gray-900 dark:text-gray-50 no-underline text-4xl inline-block" href={`https://www.${platform}.com/${username}`}>
    <Icon name={`${icon}`}/>
</a>